import { 
  
} from "../actions/types";

const initialState = {
  character1: null,
  character2: null
};

export default function(state = initialState, action) {
  switch (action.type) {
    default:
      return state;
  }
};